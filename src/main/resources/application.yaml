spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        boot:
          allow_jdbc_metadata_access: false

tenants:
  default_db:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1111
  tasks_db:
    url: jdbc:postgresql://localhost:5432/my_assistant_db
    username: postgres
    password: 1111
  coffee_db:
    url: jdbc:postgresql://localhost:5432/coffeeshop_db
    username: postgres
    password: 1111